const tagContainer=document.getElementById('tags');const message=document.getElementById('message');const escapeHtml=value=>String(value).replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;').replace(/'/g,'&#039;');fetch('/tag').then(response=>response.json()).then(data=>tagContainer.innerHTML=data.map(tag=>`<label class="tag-option"><input type="checkbox" value="${tag.id}">${escapeHtml(tag.name)}</label>`).join('')).catch(()=>tagContainer.textContent='Tags could not be loaded.');document.getElementById('form').addEventListener('submit',async event=>{event.preventDefault();try{const response=await fetch('/material',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({title:title.value.trim(),url:url.value.trim(),memo:memo.value.trim(),tagIds:Array.from(tagContainer.querySelectorAll('input:checked')).map(input=>Number(input.value))})});if(!response.ok)throw new Error();message.textContent='Material created successfully.';message.className='message success';setTimeout(()=>location.href='/materials.html',500)}catch(error){console.error(error);message.textContent='Failed to create material.'}});
