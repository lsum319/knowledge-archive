const deleteButton=document.getElementById('deleteButton');if(deleteButton)deleteButton.addEventListener('click',async event=>{if(!confirm('Delete this material?'))return;const response=await fetch(`/material/${encodeURIComponent(event.currentTarget.dataset.id)}`,{method:'DELETE',credentials:'same-origin'});if(response.ok)location.href='/materials.html';else alert('Failed to delete material.')});
